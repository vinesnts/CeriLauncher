String translate(text) {
  //
  // ----- SETUP -----
  //

  if (text == "Welcome to") return "Wilkommen in";
  if (text == "Ceri Launcher") return "Ceri Launcher";

  if (text == "Swipe left to start") return "Zum Starten nach links wischen";

  if (text == "Choose language") return "Sprache wählen";

  if (text == "Choose your style") return "Wählen Sie Ihren Stil";

  if (text == "All apps") return "Alle Apps";

  if (text == "Only favorites") return "Nur Favoriten";

  if (text == "Tray") return text;

  if (text == "You can always change this later") return text;

  if (text == "Please choose an option to continue") return text;

  if (text == "Choose your layout") return text;

  if (text == "Grid") return text;

  if (text == "Honeycomb") return text;

  if (text == "Choose your Ceri widget") return text;

  if (text == "None") return text;

  if (text == "Light analog") return text;

  if (text == "Dark analog") return text;

  if (text == "Digital") return text;

  if (text == "Assistant") return text;

  if (text == "User widget") return text;

  if (text == "Choose your package") return text;

  if (text ==
      "I'm an 18-year-old UK maths student trying to save money to study Cyber Security at university.")
    return text;
  if (text ==
      "If you want access to all features and you don't mind sparing ~\$2, I would be incredibly grateful!")
    return text;

  if (text == "Upgrade to pro") return text;
  if (text == "Support me and access all current and future features!")
    return text;

  if (text == "Restore purchase") return text;
  if (text == "Already supported the project?") return text;

  if (text == "Stay with free version") return text;
  if (text == "Core functionality will always be free") return text;

  if (text == "One last thing") return text;

  if (text ==
      "I rely completely on user feedback to help make Ceri Launcher better.")
    return "Ich verlasse mich vollständig auf das Feedback der Benutzer, um den Ceri Launcher besser zu machen.";
  if (text == "Please consider joining") return "Bitte denken Sie daran,";
  if (text == "the Ceri Launcher subreddit")
    return "dem Ceri Launcher Subreddit beizutreten";
  if (text == "or sending me an email with your thoughts.")
    return "oder mir eine E-Mail mit Ihren Gedanken zu schicken.";
  if (text == "I'd appreciate it a lot!")
    return "Ich würde es sehr zu schätzen wissen!";
  if (text == "Hope you enjoy,") return text;

  if (text == "Tap anywhere to see your new home") return text;

  //
  // ----- HOMESCREEN -----
  //

  if (text == "Swipe down to search") return text;

  if (text == "Pull down to search") return text;

  if (text == "Long-press an app for options") return text;

  if (text == "Close") return text;

  if (text == "Opens your Ceri Launcher settings") return text;

  if (text == "Ceri Launcher Settings") return text;

  if (text == "Add to favorites") return text;

  if (text == "Remove from favorites") return text;

  if (text == "Add back to homescreen") return text;

  if (text == "Hide from homescreen") return text;

  if (text == "Application info") return text;

  if (text == "Uninstall") return text;

  //
  // ----- SEARCH -----
  //

  if (text == "Search the web for")
    return text;
    
  if (text == "Search engine:") return text;

  //
  // ----- SETTINGS -----
  //

  if (text == "Set as home") return text;
  if (text == "Open default apps menu") return text;

  if (text == "Device settings") return text;
  if (text == "Open the standard settings app") return text;

  if (text == "Upgrade to pro") return text;
  if (text == "Support me and access all current and future features!") return text;

  if (text == "Restore purchase") return text;
  if (text == "Already supported the project?") return text;

  if (text == "Style") return text;
  if (text == "Choose style (All apps, Only favorites, Tray), toggle app dimming") return text;

  if (text == "Wallpapers") return text;
  if (text == "Choose wallpaper, apply dim/vignette") return text;

  if (text == "Dark mode") return text;
  if (text == "True dark mode for OLED devices\n(or #000000 lovers)") return text;

  if (text == "Configure layout") return text;
  if (text == "Standard grid or honeycomb, 2, 3 or 4 columns, customise icon size and padding") return text;

  if (text == "Icon style") return text;
  if (text == "Icon packs, shape, zoom/crop, shadows") return text;

  if (text == "Search options") return text;
  if (text == "Search icon size, search engine") return text;

  if (text == "Configure Ceri widget") return text;
  if (text == "Choose Ceri widget, configure padding") return text;

  if (text == "Scrolling animations") return text;
  if (text == "None, bubble, straight, inwards, outwards, and more") return text;

  if (text == "Notification dots") return text;
  if (text == "See indications on your homescreen for pending notifications") return text;

  if (text == "Gestures") return text;
  if (text == "Launch apps and actions quickly with side gestures") return text;

  if (text == "Miscellaneous settings") return text;
  if (text == "Warning: Some of these may not have been fully tested") return text;

  if (text == "Performance options") return text;
  if (text == "Experimental settings that may improve performance") return text;

  if (text == "About Ceri Launcher") return text;
  if (text == "Version number, developer info") return text;

  if (text == "Check for updates") return text;
  if (text == "Check for Ceri Launcher updates on the Play Store") return text;

  if (text == "Support and feedback") return text;
  if (text == "Go to the Ceri Launcher subreddit") return text;

  if (text == "Email developer") return text;
  if (text == "Have an issue or suggestion? Really enjoying Ceri Launcher? I try to reply within a few hours :)") return text;

  if (text == "Change language") return text;
  if (text == "Change the language for the Ceri Launcher interface") return text;

  if (text == "Add translations") return text;
  if (text == "Use your language skills to help make Ceri Launcher better. These translations will be added periodically in future updates.") return text;

  if (text == "Open source licenses") return text;

  if (text == "Version") return text; // context: "Version 1.7.5-beta" etc.
  if (text == "Paid — thanks!") return text;
  if (text == "Free version") return text;
  if (text == "Your own") return text;

  if (text == "Ceri Wallpapers") return text;
  if (text == "Android Home Xperience") return text;
  if (text == "Tiles below 'Your own' will open the respective wallpaper album in your browser.") return text;
  if (text == "Vignette/dim") return text;

  if (text == "Dark mode") return text;
  if (text == "Standard") return text;
  if (text == "Black (OLED)") return text;
  if (text == "Automatic") return text;
  if (text == "Dark mode off") return text;
  if (text == "Dark mode on") return text;
  if (text == "Dark mode enables at night") return text;

  if (text == "Miscellaneous") return text;
  if (text == "Disable haptics") return text;
  if (text == "Show package names in search") return text;
  if (text == "Disable vignette") return text;
  if (text == "Disable scrolling beyond screen height") return text;

  if (text == "2 columns") return text;
  if (text == "3 columns") return text;
  if (text == "Icon size") return text;
  if (text == "Icon padding") return text;
  if (text == "Standard grid layout") return text;
  if (text == "Honeycomb layout") return text;
  if (text == "Favorites stay at the top") return text;
  if (text == "Just your favorites, nothing else") return text;
  if (text == "Automatically show your most used apps only") return text;
  if (text == "(experimental)") return text;
  if (text == "Dim apps that aren't in favorites") return text;

  if (text == "Nothing set") return text;
  if (text == "Choose a package") return text;
  if (text == "Actions will be launchable with gestures in an upcoming update.") return text;
  if (text == "Search") return text;

  if (text == "No icon packs found.") return text;
  if (text == "None (default)") return text;
  if (text == "Select icon pack") return text;
  if (text == "If you have an icon pack installed, choose it here") return text;

  if (text == "Corner radius") return text;
  if (text == "Zoom/crop") return text;
  if (text == "Shadows (off)") return text;
  if (text == "Shadows (subtle)") return text;
  if (text == "Shadows (heavy)") return text;
  if (text == "Shadows (heavier)") return text;

  if (text == "App icon size") return text;

  if (text == "Animation strength") return text;
  if (text == "disabled") return text;
  if (text == "weak") return text;
  if (text == "normal") return text;
  if (text == "strong") return text;
  if (text == "strongest") return text;

  if (text == "These are EXPERIMENTAL settings to try out if you suffer from poor performance or extreme battery drain when using Ceri Launcher.") return text;
  if (text == "If you are not having issues with performance it is recommended that you leave these at their defaults.") return text;
  if (text == "Icon render size") return text;
  if (text == "Default is 192. Smaller values should result in lower RAM usage (at the expense of image quality).") return text;
  if (text == "EXAMPLE") return text;
  if (text == "Remove icons out of frame") return text;
  if (text == "Disable antialiasing") return text;
  if (text == "Force icon caching") return text;

  if (text == "Ceri Launcher utilizes the works of these people:") return text;

  if (text == "EXPERIMENTAL.") return text;

  if (text == "Nothing") return text;
  if (text == "Light analog clock widget") return text;
  if (text == "by NPKompleet, The CS Guy and Ali Sayed") return text;
  if (text == "Dark analog clock widget") return text;
  if (text == "Digital clock") return text;

  //
  // ----- WIDGETS -----
  //

  if (text == "Monday") return text;
  if (text == "Tuesday") return text;
  if (text == "Wednesday") return text;
  if (text == "Thursday") return text;
  if (text == "Friday") return text;
  if (text == "Saturday") return text;
  if (text == "Sunday") return text;

  if (text == "January") return text;
  if (text == "February") return text;
  if (text == "March") return text;
  if (text == "April") return text;
  if (text == "May") return text;
  if (text == "June") return text;
  if (text == "July") return text;
  if (text == "August") return text;
  if (text == "September") return text;
  if (text == "October") return text;
  if (text == "November") return text;
  if (text == "December") return text;

  if (text == "Good morning") return text;
  if (text == "Good afternoon") return text;
  if (text == "Good evening") return text;
  if (text == "Sleep well") return text;

  return text; // return original text if no translation found
}
